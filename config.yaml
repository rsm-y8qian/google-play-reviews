# config.yaml
data:
  raw_dir: "data/raw"
  processed_file: "data/processed/reviews.parquet"  # latest snapshot

outputs:
  runs_dir: "outputs/runs"

validation:
  required_columns:
    - review_id

store:
  parquet_prefer: true
